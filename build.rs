fn main(){
    /*std::process::Command::new("cargo")
        .args(["install", "--git", "https://github.com/mlichvar/rsntp.git"])
        .output()
        .expect("Failed to install rsntp server!");
        */
}